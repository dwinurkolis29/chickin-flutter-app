/// Theme variant selector for Chickin (BroilerKu).
///
/// Single brand — Teal Ocean (`#09637E`). No multi-color palette by design.
/// [AppTheme.build] switches between light and dark token sets only.
enum AppThemeOption { light, dark }
